import { Posts } from "@/components/Posts";
import {
  Container,
  Title,
} from "@mantine/core";

export default function page() {
  return (
    <Container>
      <Posts />
    </Container>
  );
}
